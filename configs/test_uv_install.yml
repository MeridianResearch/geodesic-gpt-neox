{
  # Test config for verifying UV environment installation
  # This is a dummy config that trains for only 200 iterations with random init
  # Hardware: Single node (4 GPUs), micro_batch=4, seq_len=2048

  # Use existing tokenized data
  "data_path": "/projects/a5k/public/data/wmdp-bio-forget-corpus_train/wmdp-bio-forget-corpus_train_text_document",
  "vocab_file": "/projects/a5k/public/data/neox_tokenizer/tokenizer.json",
  "tokenizer_type": "HFTokenizer",
  "data_impl": "mmap",

  # Logging
  "checkpoint_validation_with_forward_pass": False,
  "tensorboard_dir": "tensorboard",
  "log_dir": "logs",
  "log_interval": 1,
  "steps_per_print": 1,
  "wall_clock_breakdown": true,
  "use_wandb": True,
  "wandb_host": "https://api.wandb.ai",
  "wandb_project": "AISI",
  "wandb_team": "eleutherai",
  "wandb_run_name": "DUMMY_test_uv_install",

  # Distributed Training
  "pipe_parallel_size": 1,
  "model_parallel_size": 1,
  "launcher": "slurm",
  "deepspeed_slurm": true,
  "no_ssh_check": true,

  # Training Duration - only 200 iterations for testing
  "train_iters": 200,
  "lr_decay_iters": 200,
  "distributed_backend": "nccl",
  "lr_decay_style": "cosine",
  "warmup": 0.01,
  "split": "100,0,0",
  "eval_interval": 1000000,
  "eval_iters": 0,

  # Architecture (same as production configs)
  "num_layers": 32,
  "hidden_size": 4096,
  "num_attention_heads": 32,
  "seq_length": 2048,
  "max_position_embeddings": 2048,
  "norm": "layernorm",
  "pos_emb": "rotary",
  "rotary_pct": 0.25,
  "no_weight_tying": true,
  "gpt_j_residual": true,
  "output_layer_parallelism": "column",
  "attention_config": [[["flash"], 32]],
  "scaled_upper_triang_masked_softmax_fusion": true,
  "precision": "bfloat16",
  "activation": "gelu",

  # Transformer Engine - disabled for test (compatibility issues with TE 1.12)
  "te_columnparallel": false,
  "te_rowparallel": false,
  "te_layernorm_mlp": false,
  "te_mha": false,
  "te_fp8_format": "hybrid",
  "te_fp8_wgrad": false,
  "te_fp8_amax_history_len": 1,
  "te_fp8_amax_compute_algo": "most_recent",
  "te_fp8_margin": 0,
  "te_fp8_mha": false,

  # Optimization
  "optimizer":
    {
      "type": "Adam",
      "params": { "lr": 0.00002, "betas": [0.9, 0.95], "eps": 1.0e-8 },
    },
  "min_lr": 0,
  "zero_optimization":
    {
      "stage": 1,
      "allgather_partitions": true,
      "allgather_bucket_size": 1260000000,
      "overlap_comm": true,
      "reduce_scatter": true,
      "reduce_bucket_size": 1260000000,
      "contiguous_gradients": true,
      "cpu_offload": false,
    },
  "train_micro_batch_size_per_gpu": 4,
  "gradient_accumulation_steps": 1,
  "gradient_clipping": 1,
  "weight_decay": 0.1,
  "hidden_dropout": 0,
  "attention_dropout": 0,

  # Checkpointing - disabled for test
  "checkpoint_activations": false,
  "checkpoint_num_layers": 8,
  "partition_activations": true,
  "synchronize_each_layer": true,
  "checkpoint_factor": 1000000,
  "no_save_optim": true,
  "save": "/projects/a5k/public/tmp/test_uv_install_dummy_checkpoint",

  # NO loading - start from random initialization
  # "load": <removed>,
  # "finetune": <removed>,
}
